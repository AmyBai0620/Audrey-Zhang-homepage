# PostgreSQL数据库配置模板
# 使用方法：
# 1. 复制此文件并重命名为 application-dev.properties
# 2. 将下面的 YOUR_PASSWORD_HERE 替换为你的PostgreSQL密码
# 3. application-dev.properties 不会被提交到Git（已在.gitignore中配置）

# 数据库连接URL
spring.datasource.url=jdbc:postgresql://localhost:5432/audrey_homepage

# 数据库用户名
spring.datasource.username=postgres

# 数据库密码（请替换为你的实际密码）
spring.datasource.password=YOUR_PASSWORD_HERE

# PostgreSQL数据库驱动
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA配置
# ddl-auto=update: 自动根据实体类更新数据库表结构
spring.jpa.hibernate.ddl-auto=update

# 显示SQL语句（开发时建议开启，方便学习和调试）
spring.jpa.show-sql=true

# 格式化SQL语句，让输出更易读
spring.jpa.properties.hibernate.format_sql=true

# PostgreSQL方言
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
